import { Head } from "mdx-go";
import GlobalStyle from "../src/style/GlobalStyle.tsx";
export const Root = props => (
  <div>
    <Head>
      <title>Friendie</title>
      <link
        href="https://fonts.googleapis.com/css?family=Sue+Ellen+Francisco|Annie+Use+Your+Telescope"
        rel="stylesheet"
      />
    </Head>
    <GlobalStyle />
    {props.children}
  </div>
);

# Friendie

A social framework for building friendships
